<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//TEST_AMDTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.server.model.dao.admin.AdminMapper">
    <select id="findByUsername" resultType="User">
        SELECT * FROM EMPLOYEE;
    </select>

    <insert id="saveEmp" >
        insert into employee (username, depId, password, name, department,
                            position, email, manager, location, qrPath, img, gender, role, createAt, restTime, workingStatus)
        values (#{username}, #{name},#{email}, #{img}, #{gender},
                #{department}, #{location}, #{position}, #{manager}, #{qrPath})
    </insert>


</mapper>